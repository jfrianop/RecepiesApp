import React from 'react';
import { View, Text } from 'react-native'

export default function RecepieDetails() {
    return (
        <View>
            <Text>Detalle de una receta</Text>
        </View>
    );
}